#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mocha-es6/bin/mocha-es6.js" "$@"
  ret=$?
else 
  node  "$basedir/../mocha-es6/bin/mocha-es6.js" "$@"
  ret=$?
fi
exit $ret
